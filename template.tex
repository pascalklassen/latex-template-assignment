\documentclass[12pt]{article}

% █▓▒▒░░░Language / Encoding░░░▒▒▓█

\usepackage[ngerman]{babel}
\usepackage[T1]{fontenc}

% █▓▒▒░░░Formatting░░░▒▒▓█

\usepackage[hmargin=2cm, vmargin=3cm]{geometry}
\geometry{a4paper}

% █▓▒▒░░░Title / Header / Footer░░░▒▒▓█

\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}
\lhead{}
\chead{Titel\\Untertitel}
\rhead{\today}
\rfoot{Seite \thepage}
\lfoot{Fach}

\renewcommand{\headrulewidth}{2pt}
\renewcommand{\footrulewidth}{1pt}

% █▓▒▒░░░Font / Typefacing░░░▒▒▓█

\usepackage{lmodern}
\renewcommand*\familydefault{\sfdefault} %% Only if the base font of the document is to be sans serif

% █▓▒▒░░░Listings / Code Style░░░▒▒▓█

\usepackage{listings}
\usepackage{xcolor}

\definecolor{background}{HTML}{fdf6e3}
\definecolor{text}{HTML}{586e75}
\definecolor{comments}{HTML}{93A1A1}
\definecolor{keywords}{HTML}{859900}
\definecolor{strings}{HTML}{93a1a1}
\definecolor{rule}{HTML}{edead9}

\lstset{ %
	backgroundcolor=\color{background},              % choose the background color; you must add \usepackage{color} or \usepackage{xcolor}
	basicstyle=\small \ttfamily \color{text},        % the size of the fonts that are used for the code
	breakatwhitespace=false,                         % sets if automatic breaks should only happen at whitespace
	breaklines=true,                                 % sets automatic line breaking
	captionpos=t,                                    % sets the caption-position to bottom
	commentstyle=\color{comments}\textit,            % comment style
	extendedchars=true,                              % lets you use non-ASCII characters; for 8-bits encodings only, does not work with UTF-8
	frame=single,	                                 % adds a frame around the code,
	keepspaces=true,                                 % keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
	keywordstyle=\color{keywords}\bfseries,          % keyword style
	numbers=left,                                    % where to put the line-numbers; possible values are (none, left, right)
	numbersep=7pt,                                   % how far the line-numbers are from the code
	numberstyle=\tiny\color{comments},               % the style that is used for the line-numbers
	rulecolor=\color{rule},                          % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. comments (green here))
	showspaces=false,                                % show spaces everywhere adding particular underscores; it overrides 'showstringspaces'
	showstringspaces=false,                          % underline spaces within strings only
	showtabs=false,                                  % show tabs within strings adding particular underscores
	stepnumber=1,                                    % the step between two line-numbers. If it's 1, each line will be numbered
	stringstyle=\color{strings},                     % string literal style
	tabsize=2,	                                     % sets default tabsize to 2 spaces
	columns=fixed                                    % Using fixed column width (for e.g. nice alignment)
}

% █▓▒▒░░░Packages░░░▒▒▓█

\usepackage{amsmath}
\usepackage{amssymb}

\begin{document}
	Hello, World!
\end{document}
